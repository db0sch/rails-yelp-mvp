<%= simple_form_for([@restaurant, @review]) do |f| %>
  <%= f.error_notification %>

  <%= f.input :content %>
  <%= f.input :rating, as: :select, collection: 0..5, include_blank:  "your rating" %>

  <%= f.button :submit %>
<% end %>